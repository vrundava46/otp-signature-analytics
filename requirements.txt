# Core dependencies — light, reliable, everything runs on these.
numpy>=1.24
duckdb>=0.10
pydantic>=2.5
typer>=0.9
faker>=22.0
rich>=13.0
requests>=2.28
streamlit>=1.30
pandas>=2.0
pytest>=7.4

# Optional enhancement (better semantic embeddings; pulls in torch, ~hundreds MB):
#   pip install -r requirements-embeddings.txt
# The project auto-detects it and otherwise uses a built-in hashing embedder.
